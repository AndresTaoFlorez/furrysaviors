// import React from "react";
// import { LoginProvider } from '../context/LoginContext';
// import { ConfigContextProvider } from '../context/ConfigContext';
// // Importa más contextos según sea necesario

// const combineProviders = (...providers) => ({ children }) =>
//   // El primer element, o iniciador, es childre, luego se sigue anidando con los providers
//   providers.reduceRight(
//     (acc, Provider) => <Provider>{acc}</Provider>,
//     children
//   );

// const AppProviders = combineProviders(LoginProvider, ConfigContextProvider);

// export default AppProviders;
